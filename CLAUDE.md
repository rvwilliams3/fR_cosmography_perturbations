# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

Mathematica notebooks for a cosmography-based f(R) modified gravity project. The approach parametrises the background cosmology via cosmographic variables (deceleration q, jerk j, snap s) rather than specifying f(R) directly, and computes observables (fσ8 growth rate) to compare against ΛCDM and data.

## Notebook execution order

The notebooks have a dependency chain and must be run in order:

1. **`Eos_energymomentum.nb`** — Derives symbolic expressions for the f(R) field equations, background energy-momentum tensor, and equation of state using the xAct tensor calculus package. Run first to establish the symbolic scaffolding.

2. **`Background solutions/Determining ICs.nb`** — Determines initial conditions at z₀=6 by propagating from the q value at z=0.

3. **`Background solutions/Phase Space Analysis all models.nb`** — Explores the phase space trajectories using those initial conditions to check physical viability. Does not depend on any `.mx` files.

4. **`Background solutions/Solving Background all models.nb`** — Numerically solves the autonomous background system for Models 0–3 and ΛCDM. Saves results to `.mx` binary files (`BackgroundSols_model0.mx`, etc.) which are gitignored.

5. **`Cosmological perturbations/Perturbation equations QS.nb`** — Solves the quasi-static matter perturbation ODE, loads background `.mx` files, computes fσ8 for all models, and plots against observational data in `fsigma8.txt`.

## Key variables and naming

The background autonomous system is expressed in redshift z with three dimensionless variables:

- `x[z]` — cosmographic phase-space variable (related to jerk/deceleration)
- `Ω[z]` — matter density parameter Ω_m(z)  
- `q[z]` — deceleration parameter
- `hz[z]` — dimensionless Hubble parameter H(z)/H₀ (named `hz` not `h` to avoid conflict with `h` in H₀ = 100h)

Models are labelled 0–3 corresponding to different parametrisations of the jerk j(z). Solutions are stored as `sol0`, `sol1`, `sol2`, `sol3`, `solLCDM` and replacement rules `LCDMreplacements`.

## External dependencies (Mathematica)

- **xAct** (`xPerm`, `xTensor`, `xCoba`) — tensor calculus; required only for `Eos_energymomentum.nb`
- `FunctionApproximations\`` — used in the perturbation notebook
- `DifferentialEquations\`NDSolveProblems\`` — used in the perturbation notebook
- `ErrorBarPlots\`` — for fσ8 plots with error bars (note: flagged obsolete in Mathematica 14, but still loads)

All notebooks begin with `Quit` then `SetDirectory @ NotebookDirectory[]` — evaluate from the top each session.

## Data files

- `Cosmological perturbations/fsigma8.txt` — observational fσ8 measurements (columns: redshift, f·σ8, error)
- `*.mx` files — binary Mathematica dump files of numerical solutions; gitignored, must be regenerated by running the background solver

## Repo structure

```
Background solutions/       Background ODE solver, phase space analysis, IC determination
Cosmological perturbations/ QS perturbation solver, fσ8 computation and plotting
Eos_energymomentum.nb       Symbolic derivation of field equations (xAct)
old code/                   Archived notebooks; gitignored
```
